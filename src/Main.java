void main() {
    int quantidadeDePassos = 500;
    double alturaEmMetros = 1.75;
    //Tipo referenciado: são referencias para uma posição na memoria, não guardam exatamente o valor
    String nome = "Jose";
    String sugestao = "Sugiro que você tente aumentar a meta!";
    if(quantidadeDePassos < 1000) {
        System.out.println(sugestao);
    }
}
